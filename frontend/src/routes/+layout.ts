// SPA pura: sem SSR, sem prerender — toda a aplicação roda no navegador,
// falando com a API Go via fetch (ver refatoracao/06-frontend.md, "Stack
// fixado").
export const ssr = false;
export const prerender = false;
